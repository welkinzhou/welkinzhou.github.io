---
slug: 20260817
title: Python 数据库操作
authors: zhouyuan
tags: [python, sqlalchemy, PostgreSQL]
---

补一点基础，主要是数据库相关。

<!-- truncate -->

## SQLAlchemy 介绍

SQLAlchemy 是 Python 生态中最成熟、功能最全面的数据库工具包，它提供了从底层 SQL 操作到高级 ORM 的完整数据库访问能力，支持 **PostgreSQL、MySQL、SQLite、Oracle、SQL Server** 等主流数据库，通过统一 API 操作不同数据库，切换数据库只需改连接字符串。PostgreSQL 可以通过 pgvector 拓展，增加向量数据类型支持，效率上不如 Milvus 这种专业向量数据库。

## ORM

ORM（Object-Relational Mapping），会把对象操作，映射到数据库，基本使用如下：

```python
from sqlalchemy import create_engine, String
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column

# 创建引擎（连接数据库）
engine = create_engine("postgresql+psycopg2://用户名:密码@主机:端口/数据库名", echo=True)

class Base(DeclarativeBase):
    pass  # 空类即可，元类在背后工作

class UUIDPrimaryKeyMixin:
    id: Mapped[uuid.UUID] = mapped_column(
        PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4
    )

class User(Base, UUIDPrimaryKeyMixin):
    # 表名
    __tablename__ = "users"
    name: Mapped[str] = mapped_column(String(50))
    email: Mapped[str | None] = mapped_column(default=None)

# 创建表
Base.metadata.create_all(engine)

# 会话操作
with Session(engine) as session:
    # 新增：对象 → 自动转成 INSERT SQL
    user = User(name="李四", email="li@example.com")
    session.add(user)      # 加入会话（追踪状态）
    session.commit()       # 自动执行 INSERT
    
    # 查询：自动转成 SELECT SQL
    result = session.execute(
        select(User).where(User.name == "李四")
    )
    u = result.scalar_one()
    
    # 修改：Session 自动追踪变化 → 自动执行 UPDATE
    u.email = "new@example.com"
    session.commit()       # 自动执行 UPDATE
    
    # 删除
    session.delete(u)
    session.commit()       # 自动执行 DELETE
```

通常我们使用数据库，要用 `INSERT` 等语句。通过定义 ORM 对象，SQLAlchemy 会帮我们代理数据库操作。常见操作：

```python
from sqlalchemy import select, insert, update, delete, func
from sqlalchemy.orm import Session, joinedload, selectinload

with Session(engine) as session:
    
    # ---------- 查 ----------
    # 按主键
    user = session.get(User, 1)
    
    # 单个（确定有）
    user = session.execute(select(User).where(User.id == 1)).scalar_one()
    
    # 单个（可能有）
    user = session.execute(select(User).where(User.email == "a@b.com")).scalar_one_or_none()
    
    # 列表
    users = session.execute(select(User).where(User.age > 18)).scalars().all()
    
    # 第一个
    user = session.execute(select(User)).scalars().first()
    
    # 计数
    count = session.execute(select(func.count()).select_from(User)).scalar()
    
    # 关联预加载（防 N+1）
    users = session.execute(
        select(User).options(selectinload(User.posts))
    ).scalars().all()
    
    # ---------- 增 ----------
    session.add(User(name="张三"))
    session.add_all([user1, user2])
    session.commit()
    
    # 批量插入（Core 方式）
    session.execute(insert(User), [{"name": "A"}, {"name": "B"}])
    session.commit()
    
    # ---------- 改 ----------
    user.name = "新名字"        # 改对象属性
    session.commit()            # 自动 UPDATE
    
    # 批量改
    session.execute(update(User).where(User.id == 1).values(name="新名字"))
    session.commit()
    
    # ---------- 删 ----------
    session.delete(user)
    session.commit()
    
    # 批量删
    session.execute(delete(User).where(User.id == 1))
    session.commit()
```

`Mapped` 注释属性类型，`mapped_column` 告诉 sqlalchemy 在数据库要做什么操作。以 `id` 为例，`Mapped[uuid.UUID]` python 就知道这个属性应该是 uuid 类型的。`mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)` 告诉 sqlalchemy，数据库中 id 是主键，类型 uuid，默认值。

**`mapped_column` 常用参数**

| 参数                 | 说明                                          | 示例                                          |
| :------------------- | :-------------------------------------------- | :-------------------------------------------- |
| **`primary_key`**    | 是否主键                                      | `mapped_column(primary_key=True)`             |
| **`nullable`**       | 是否允许 NULL（默认根据 `Mapped[类型]` 推断） | `mapped_column(nullable=False)`               |
| **`default`**        | Python 默认值（插入时生成）                   | `mapped_column(default=0)`                    |
| **`server_default`** | 数据库默认值（由数据库生成）                  | `mapped_column(server_default=text("now()"))` |
| **`index`**          | 是否创建索引                                  | `mapped_column(index=True)`                   |
| **`unique`**         | 是否唯一约束                                  | `mapped_column(unique=True)`                  |
| **`autoincrement`**  | 是否自增（主键整型默认 True）                 | `mapped_column(autoincrement=True)`           |
| **`comment`**        | 字段注释                                      | `mapped_column(comment="用户姓名")`           |
| **`type_`**          | 显式指定列类型（通常自动推导）                | `mapped_column(type_=String(100))`            |
| **`ForeignKey`**     | 外键关联                                      | `mapped_column(ForeignKey("users.id"))`       |
| **`onupdate`**       | 更新时自动赋值                                | `mapped_column(onupdate=func.now())`          |

## 事务封装

事务，简单理解就是批量操作，同一批操作可以共同管理。常见场景就是一个操作失败，所有操作都回滚。以商品入库为例，入库后需要更新调拨单信息，更新库存、产品信息。这些操作依次进行，调拨单标记完成后，发现库存信息对不上，入库操作就不能进行，但是单据信息流转已经完成了。这种情况就需要报错一起回滚。

下面是一个简单事务处理封装，所有的事务提交都聚合在这里：

```python
@asynccontextmanager
async def session_scope(
    factory: async_sessionmaker[AsyncSession],
) -> AsyncIterator[AsyncSession]:
    """唯一事务边界：成功提交，失败回滚。HTTP / Celery / eval 均经此进入。"""
    async with factory() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
            
# 使用时候
async with session_scope(session_factory) as session:
  # 进行操作
  user = User(name="李四", email="li@example.com")
  session.add(user)
  # 事务不提交，更新数据库预览单次结果
  session.flush()
```

`commit` 会直接提交。如果我们分步操作，每步完成校验，可以使用 `flush`。flush 会将数据修改提交到数据库，事务不会被提交，出现失败，`session.rollback()` 会自动回滚。

`asynccontextmanager` 会创建一个异步上下文，session 是每次服务临时创建的数据库链接，请求完成后就需要释放。手动管理太麻烦，使用 asynccontextmanager 创建上下文，外部调用使用 `with` 关键字，语句执行完成自动释放。
